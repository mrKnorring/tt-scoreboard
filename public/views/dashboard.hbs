<header class="flex w-full mt-4 justify-center items-center relative">
	<h1 class="text-3xl font-bold m-auto">Välj bana</h1>
	<div class=" absolute right-4 bottom-2">
		<a href='/login'>Logga in</a>
	</div>
</header>

<main class="max-w-screen-xl m-auto mt-24 flex justify-center">
	{{#each users}}
	<a href="/court/{{userId}}">
		<div class="rounded-md bg-slate-200 py-4 px-8  hover:cursor-pointer">
			<h2 class="text-2xl font-bold">{{name}}</h2>
			<div class="text-lg">#{{userId}}</div>
		</div>
	</a>
	{{/each }}
</main>